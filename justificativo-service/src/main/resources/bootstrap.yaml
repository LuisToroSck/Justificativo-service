spring:
  application:
    name: justificativo-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:mysql://localhost:3307/justificativo_db
    username: root
    password: LuchoSick17
  jpa:
    hibernate:
      ddl-auto: update

management.endpoints.web.exposure.include = *      